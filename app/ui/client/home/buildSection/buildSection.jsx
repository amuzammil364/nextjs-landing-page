import styles from "./buildSection.modulel.css";

const BuildSection = () => {
  return (
    <section className={styles.build_video_section}>
      <div className={styles.container}></div>
    </section>
  );
};

export default BuildSection;
